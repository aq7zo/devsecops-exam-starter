# Anything the Linux CI runner executes must be LF: a CRLF shell script fails
# there with "bad interpreter: /usr/bin/env bash^M", whatever OS committed it.
* text=auto eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.json text eol=lf
